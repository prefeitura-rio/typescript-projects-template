// example.ts — Replace this file with your actual library code.
//
// This file exists solely to demonstrate the structure:
//   - Functions live in src/lib/
//   - They are exported from src/index.ts (the public API)
//   - Tests live in tests/ and import from src/

/**
 * Adds two numbers together.
 *
 * @param a - First operand.
 * @param b - Second operand.
 * @returns The sum of a and b.
 */
export function add(a: number, b: number): number {
  return a + b
}
